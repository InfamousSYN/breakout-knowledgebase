site_name: Breakout CheatSheet
repo_url: https://github.com/InfamousSYN/breakout-knowledgebase
repo_name: InfamousSYN/breakout-knowledgebase
theme:
  name: material
  palette:
    scheme: slate # Dark mode is better for pentesting docs
    primary: red
  features:
    - navigation.sections
    - navigation.expand
    - search.suggest
    - search.highlight
    - content.code.copy

plugins:
  - search:
      separator: '[\s\-\.]+' # Helps search find "CTRL+O" even if typed "CTRL-O"

markdown_extensions:
  - toc:
      permalink: true # Adds clickable anchors to every header
  - pymdownx.highlight:
      anchor_linenums: true
  - pymdownx.inlinehilite
  - pymdownx.snippets
  - pymdownx.superfences
  - admonition
  - pymdownx.details # Allows for collapsible "spoiler" blocks
  - pymdownx.superfences # Required for nesting content

extra_css:
  - stylesheets/link-preview.css

extra_javascript:
  - javascripts/link-preview.js
